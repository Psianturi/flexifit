# ========================================
# FlexiFit Backend Configuration
# ========================================
# Copy this file to .env and fill in your values

# Google Gemini API Key
# Get yours from: https://ai.google.dev/
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Opik Configuration (Optional)
# API Key from: https://www.comet.com/opik
OPIK_API_KEY=your_opik_api_key_here

# Optional: Opik project routing
# OPIK_WORKSPACE=your_workspace
OPIK_PROJECT_NAME=flexifit-hackathon

# Track prompt iterations/experiments in Opik dashboards
PROMPT_VERSION=v1

# Gemini model (kept here for transparency; the backend prefers a Flash model)
GEMINI_MODEL=gemini-3-flash

# Self-correction loop (LLM-as-judge driven)
# If enabled and empathy_score < threshold, the backend will rewrite once.
RETRY_ON_LOW_EMPATHY=false
RETRY_EMPATHY_THRESHOLD=3

# Backend Port (default: 8000)
PORT=8000

# CORS Origins (comma-separated for production)
# For development: "*" allows all
# For production: restrict to frontend domain
CORS_ORIGINS=*


